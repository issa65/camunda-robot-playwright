server:
  port: 8082

spring:
  application:
    name: order-service

camunda:
  client:
    mode: self-managed
    auth:
      method: none
    rest-address: http://localhost:8080
    grpc-address: http://localhost:26500